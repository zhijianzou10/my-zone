const list = document.getElementById('comments_list');

list.insertAdjacentHTML("afterbegin", "<%= j render 'comment', comment: @comment %>")

// list.insertAdjacentHTML("afterbegin", "<%= j render 'components/comment_show', comment: @comment %>")


